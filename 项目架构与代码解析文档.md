# ESP32 BLDC 电机 FOC 控制 — 项目架构与代码解析文档

> 项目名称: `motor_control_1.0`  
> 平台: ESP32 (ESP-IDF)  
> 核心功能: 基于磁场定向控制 (FOC) 的 BLDC 电机驱动  
> 对标参考: [SimpleFOC / Arduino-FOC](https://docs.simplefoc.com/) 算法移植

---

## 目录

1. [项目概览](#1-项目概览)
2. [整体架构](#2-整体架构)
3. [分层架构详解](#3-分层架构详解)
   - [3.1 应用入口层 (`main/`)](#31-应用入口层-main)
   - [3.2 集成控制模块 (`icm/`)](#32-集成控制模块-icm)
   - [3.3 FOC 算法层 (`algorithm/`)](#33-foc-算法层-algorithm)
   - [3.4 电机驱动层 (`moter_driver/`)](#34-电机驱动层-moter_driver)
   - [3.5 传感器驱动层 (`sensors/`)](#35-传感器驱动层-sensors)
4. [数据流与控制流](#4-数据流与控制流)
5. [文件清单与作用说明](#5-文件清单与作用说明)
6. [核心代码解析](#6-核心代码解析)
7. [FOC 基本原理概述](#7-foc-基本原理概述)
8. [构建系统说明](#8-构建系统说明)

---

## 1. 项目概览

本工程是一个基于 ESP32 平台的 **三相 BLDC 电机 FOC（磁场定向控制）** 系统，采用 ESP-IDF 框架构建。核心算法对标开源项目 **SimpleFOC**（Arduino-FOC 库），将其 C++ 面向对象架构重写为纯 C 实现，适配 ESP-IDF 组件化构建系统。

### 核心特性

| 特性 | 说明 |
|------|------|
| **控制模式** | 力矩控制 (电压/电流)、速度闭环、角度闭环 (级联 PID) |
| **调制方式** | SVPWM、正弦 PWM、梯形波 120°/150° |
| **传感器** | AS5600 磁编码器 (I2C)，支持抽象层扩展 |
| **电机参数辨识** | 自动测量相电阻、相电感 |
| **电流环自整定** | 基于带宽的 PI 参数自动计算 |
| **传感器对齐** | 自动检测方向 + 电零点校准 |
| **架构模式** | 分层解耦 + 回调注入 + 不透明指针 |

---

## 2. 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        main.c (应用入口)                         │
│  不依赖任何底层头文件，仅通过 motor_control.h 交互                │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                icm/ (集成控制模块 — motor_control.c)              │
│  协调硬件驱动、算法、传感器，注入回调函数，暴露统一 API            │
└──────┬──────────────┬────────────────┬──────────────────────────┘
       │              │                │
       ▼              ▼                ▼
┌──────────┐  ┌──────────────┐  ┌──────────────────────────┐
│ sensors/ │  │ moter_driver/│  │  algorithm/              │
│          │  │              │  │                          │
│ AS5600   │  │ bldc_motor   │  │ foc.c      (FOC 控制逻辑)│
│ 磁编码器 │  │ (LEDC PWM +  │  │ pid.c      (PID 控制器)  │
│ (I2C)    │  │  GPIO 使能)  │  │ lowpass    (低通滤波)    │
│          │  │              │  │ foc_utils  (三角函数/工具)│
│          │  │ bldc_foc_    │  │ foc_defaults (默认参数)   │
│          │  │ modulation   │  │                          │
│          │  │ (SVPWM/正弦/ │  │                          │
│          │  │  梯形波调制) │  │                          │
└──────────┘  └──────────────┘  └──────────────────────────┘
```

### 分层设计原则

1. **硬件抽象** — 传感器通过函数指针表解耦，驱动通过配置结构体解耦
2. **算法与平台分离** — FOC 算法不依赖任何 ESP-IDF 头文件，通过回调注入平台相关函数
3. **不透明指针** — `motor_control_t` / `sensor_t` 内部结构对上层隐藏
4. **单向依赖** — 上层依赖下层，下层不反向依赖上层

---

## 3. 分层架构详解

### 3.1 应用入口层 (`main/`)

#### 文件: `main/main.c`

**角色**: 程序入口，**零耦合**——不引用任何底层硬件或算法头文件。

**核心逻辑**:

```c
void app_main(void) {
    // 1. 一站式初始化（硬件 + 算法 + 传感器全部内部完成）
    motor_control_t *mc = NULL;
    ESP_ERROR_CHECK(motor_control_init(&mc));

    // 2. 配置控制模式：力矩控制 + 电压控制
    motor_control_set_control_type(mc, MC_CTRL_TORQUE);
    motor_control_set_torque_type(mc, MC_TORQUE_VOLTAGE);
    motor_control_set_target(mc, 1.0f);

    // 3. 执行校准流程
    ESP_ERROR_CHECK(motor_control_calibrate(mc));

    // 4. 启动 1kHz 定时控制任务
    xTaskCreate(foc_control_task, "foc_ctrl", 4096, mc, 5, NULL);
}
```

**控制任务** (`foc_control_task`): 以 1kHz 频率调用 `motor_control_step(mc)`，内部先执行电流环 (FOC 计算 + PWM 输出)，每 10 次执行一次运动外环。

---

### 3.2 集成控制模块 (`icm/`)

这是连接应用层与底层各模块的**胶水层**，负责组装硬件驱动、传感器、算法，并提供统一 API。

#### 文件: `icm/inc/motor_control.h`

**对外暴露的类型和 API**:

| 类型/函数 | 说明 |
|-----------|------|
| `mc_control_type_t` | 控制模式枚举：力矩/速度/角度 |
| `mc_torque_type_t` | 力矩类型：电压控制 |
| `motor_control_t` | 不透明指针，内部结构隐藏 |
| `motor_control_init()` | 一站式初始化 |
| `motor_control_deinit()` | 反初始化 |
| `motor_control_calibrate()` | 执行校准 |
| `motor_control_step()` | 执行一次控制循环 |
| `motor_control_enable/disable()` | 使能/禁用电机 |
| `motor_control_set_target()` | 设置目标值 |
| `motor_control_set_control_type()` | 切换控制模式 |
| `motor_control_set_torque_type()` | 切换力矩类型 |

#### 文件: `icm/src/motor_control.c`

**内部结构**:

```c
struct motor_control_t {
    bldc_motor_config_t motor_cfg;  // 电机驱动硬件配置
    foc_context_t        foc;       // FOC 算法上下文
    sensor_t            *sensor;    // 传感器抽象句柄
};
```

**核心设计 — 回调注入**: `foc_inject_callbacks()` 将平台相关的函数指针注入到 FOC 上下文中，使算法层保持平台无关：

| 回调 | 绑定函数 | 作用 |
|------|---------|------|
| `delay_ms` | `vTaskDelay` | 毫秒延时 |
| `delay_us` | `esp_rom_delay_us` | 微秒延时 |
| `get_time_us` | `esp_timer_get_time` | 获取时间戳 |
| `set_phase_voltage` | `bldc_foc_set_voltage` | 输出三相电压 |
| `sensor_update` | `sensor_update` | 传感器采样 |
| `sensor_get_angle` | `sensor_get_angle_rad` | 获取角度 |
| `sensor_get_velocity` | `sensor_get_velocity_rad` | 获取角速度 |
| `driver_enable/disable` | `bldc_motor_enable/disable` | 电机使能 |

**初始化流程** (`motor_control_init`):
```
传感器初始化 → 电机驱动初始化 → FOC 算法初始化 → 回调注入
```
任一步骤失败则回滚已初始化的模块并释放内存。

**控制步进** (`motor_control_step`):
```
foc_loop_foc()     ← 每次调用执行（电流环 1kHz）
foc_move(NAN)      ← 每 10 次执行一次（运动外环 100Hz）
```

#### 文件: `icm/inc/sensor.h` & `icm/src/sensor.c`

**传感器硬件抽象层 (HAL)**，通过**函数指针表**实现硬件隔离：

```c
typedef struct {
    esp_err_t (*init)(void);
    esp_err_t (*deinit)(void);
    esp_err_t (*update)(void);
    float (*get_angle_rad)(void);
    float (*get_velocity_rad)(void);
    bool (*needs_search)(void);
} sensor_ops_t;
```

**扩展机制**: 新增传感器只需：
1. 编写新驱动（如 `AS5048A.c`）
2. 在 `sensor.c` 中添加对应的 `sensor_ops_init_as5048a()` 函数
3. 在 `sensor_init()` 的 `switch` 中添加 `case SENSOR_TYPE_AS5048A`

当前支持 `SENSOR_TYPE_AS5600`，预留了 `AS5048A` / `MT6701` 枚举值。

---

### 3.3 FOC 算法层 (`algorithm/`)

纯算法实现，不依赖任何 ESP-IDF 硬件接口，通过回调函数与平台交互。

#### 文件: `algorithm/inc/foc.h`

**核心数据结构 — `foc_context_t`** (对标 SimpleFOC 的 `FOCMotor` 类):

```c
typedef struct foc_ctx_t {
    // 电机参数
    foc_motor_params_t motor;          // 极对数、电阻、电感、KV

    // 控制模式
    foc_control_type_t control_type;   // 力矩/速度/角度/开环
    foc_torque_type_t torque_type;     // 电压/DC电流/FOC电流/估算电流

    // 限幅
    float voltage_limit;               // 电压上限
    float velocity_limit;              // 速度上限
    float current_limit;               // 电流上限

    // 状态变量
    float shaft_angle;                 // 当前机械角度
    float shaft_velocity;              // 当前机械角速度
    float electrical_angle;            // 当前电角度
    foc_dq_voltage_t voltage;          // dq 电压
    foc_dq_current_t current;          // dq 电流

    // PID 控制器 (4 个)
    pid_controller_t pid_current_q;    // q 轴电流 PID
    pid_controller_t pid_current_d;    // d 轴电流 PID
    pid_controller_t pid_velocity;     // 速度环 PID
    pid_controller_t pid_angle;        // 角度环 P

    // 低通滤波器 (4 个)
    lpf_t lpf_current_q;               // q 轴电流滤波
    lpf_t lpf_current_d;               // d 轴电流滤波
    lpf_t lpf_velocity;                // 速度滤波
    lpf_t lpf_angle;                   // 角度滤波

    // 回调函数指针 (外部注入)
    void (*sensor_update)(void);
    float (*sensor_get_angle)(void);
    float (*sensor_get_velocity)(void);
    void (*set_phase_voltage)(float Uq, float Ud, float angle_el);
    void (*delay_ms)(uint32_t ms);
    void (*driver_enable)(void);
    // ... 更多回调
} foc_context_t;
```

**关键 API**:

| 函数 | 对标 SimpleFOC | 说明 |
|------|---------------|------|
| `foc_init()` | `FOCMotor::init()` | 初始化 PID + LPF + 默认参数 |
| `foc_init_foc()` | `FOCMotor::initFOC()` | 校准流程 (传感器对齐 + 电流传感器对齐) |
| `foc_loop_foc()` | `FOCMotor::loopFOC()` | 电流环 (dq 计算 → 调制 → PWM 输出) |
| `foc_move()` | `FOCMotor::move()` | 运动外环 (级联 PID 控制) |
| `foc_align_sensor()` | `FOCMotor::alignSensor()` | 传感器方向检测 + 电零点校准 |
| `foc_characterise_motor()` | — | 电机参数自动辨识 |

#### 文件: `algorithm/src/foc.c`

**电流环 (`foc_loop_foc`)** — 每次调用的核心流程:

```
1. 更新传感器数据
2. 计算电角度 (闭环模式从传感器读取，开环模式自行推算)
3. 根据力矩类型计算 dq 电压:
   ├── FOC_TORQUE_VOLTAGE:     直接设置 Vq = target, Vd = 0
   ├── FOC_TORQUE_ESTIMATED_CURRENT:  Vq = I*R + BEMF
   ├── FOC_TORQUE_DC_CURRENT:   电流环 PI + d 轴补偿
   └── FOC_TORQUE_FOC_CURRENT:  双 PI 电流环 + dq 交叉耦合补偿
4. 调用 set_phase_voltage(Uq, Ud, el_angle) → 输出 PWM
```

**运动外环 (`foc_move`)** — 级联 PID 结构:

```
FOC_CTRL_TORQUE:    target → current_sp
FOC_CTRL_VELOCITY:  target → [速度 PID] → current_sp
FOC_CTRL_ANGLE:     target → [角度 P] → velocity_sp → [速度 PID] → current_sp
FOC_CTRL_ANGLE_NOCASCADE: target → [角度 P] → current_sp (无速度级联)
FOC_CTRL_VELOCITY_OPENLOOP: 开环速度推算角度
FOC_CTRL_ANGLE_OPENLOOP:    开环角度追踪
```

**传感器对齐 (`foc_align_sensor`)** — 校准流程:

1. **索引搜索** (如需要): 旋转电机直到找到编码器索引信号
2. **方向检测**: 正转 → 反转，比较传感器角度变化，确定 CW/CCW
3. **极对数校验**: 检查转动一圈是否对应 `pole_pairs * 2π` 电角度
4. **电零点校准**: 在固定电角度通电锁定转子，记录当前传感器读数作为偏移

**电机参数辨识 (`foc_characterise_motor`)**:
- **电阻测量**: 斜坡升压 → 读取 d 轴电流 → `R = V / I`，估算相电阻 = `2R`
- **电感测量**: 施加固定电压脉冲 200μs → 测量 d 轴电流上升 → `L = -R*dt/ln(V_eff/V)`

#### 文件: `algorithm/inc/pid.h` & `algorithm/src/pid.c`

**PID 控制器**，采用 **Tustin 积分**（双线性变换）+ **抗积分饱和** + **输出变化率限制**：

```
P 项: proportional = Kp * error
I 项: integral = integral_prev + Ki * dt * 0.5 * (error + error_prev)  // Tustin
D 项: derivative = Kd * (error - error_prev) / dt
输出: output = constrain(P + I + D, ±limit)
变化率限制: |output_rate| > ramp → 限制为 ±ramp * dt
```

4 个 PID 实例用于: q 轴电流、d 轴电流、速度环、角度环。

#### 文件: `algorithm/inc/lowpass_filter.h` & `algorithm/src/lowpass_filter.c`

**一阶低通滤波器**：

```
alpha = Tf / (Tf + dt)
y = alpha * y_prev + (1 - alpha) * x
```

- `Tf = 0` → 不滤波 (y = x)
- `Tf` 越大 → 截止频率越低
- `dt > 0.3s` → 重置 (防止长时间未调用后跳变)

#### 文件: `algorithm/inc/foc_utils.h` & `algorithm/src/foc_utils.c`

**数学工具库**，提供嵌入式优化的三角函数和数值运算：

| 函数 | 实现方式 | 精度 |
|------|---------|------|
| `foc_sin()` | 65 元素查表 + 线性插值 | RMS ~0.000065 |
| `foc_cos()` | sin(a + π/2) | 同上 |
| `foc_sincos()` | 同时计算 sin/cos | 同上 |
| `foc_atan2()` | 多项式近似 (Odrive 方案) | ~0.001 rad |
| `foc_sqrt_approx()` | Fast Inverse Square Root | 近似 |
| `foc_normalize_angle()` | fmodf + 回绕处理 | 精确 |
| `foc_constrain()` | 数值限幅 | 精确 |

**数据结构**:

```c
typedef struct { float d; float q; } foc_dq_t;         // dq 坐标系变量
typedef struct { float alpha; float beta; } foc_ab_t;   // αβ 坐标系变量
typedef struct { float a; float b; float c; } foc_phase_current_t; // 三相变量
```

#### 文件: `algorithm/inc/foc_defaults.h`

**默认参数汇总**:

| 参数 | 值 | 说明 |
|------|-----|------|
| `FOC_DEF_POWER_SUPPLY` | 12.0V | 默认母线电压 |
| `FOC_DEF_PID_VEL_P/I/D` | 0.5 / 10.0 / 0.0 | 速度环 PID |
| `FOC_DEF_PID_CURR_P/I/D` | 3.0 / 300.0 / 0.0 | 电流环 PID |
| `FOC_DEF_P_ANGLE_P` | 20.0 | 角度环 P |
| `FOC_DEF_VEL_LIM` | 20.0 rad/s | 速度限幅 |
| `FOC_DEF_CURRENT_LIM` | 2.0 A | 电流限幅 |
| `FOC_DEF_VOLTAGE_SENSOR_ALIGN` | 3.0V | 传感器对齐电压 |
| `FOC_DEF_INDEX_SEARCH_VELOCITY` | 1.0 rad/s | 索引搜索速度 |
| `FOC_DEF_VEL_FILTER_TF` | 0.005s | 速度滤波时间常数 |
| `FOC_DEF_MOTION_DOWNSAMPLE` | 0 | 运动环降采样 (0=不降采样) |

---

### 3.4 电机驱动层 (`moter_driver/`)

#### 文件: `moter_driver/inc/bldc_motor.h` & `moter_driver/src/bldc_motor.c`

**角色**: 三相 PWM 驱动器的硬件抽象，使用 ESP32 的 LEDC (LED PWM Controller) 外设。

**配置结构体 `bldc_motor_config_t`**:

| 字段 | 说明 | 默认值 |
|------|------|--------|
| `pwm_pin_u/v/w` | 三相 PWM 输出引脚 | 10/11/12 |
| `enable_pin_u/v/w` | 三相独立使能引脚 (NC=不使用) | 4/NC/NC |
| `enable_active_high` | 使能引脚有效电平 | true |
| `voltage_power_supply` | 母线电压 | 12.0V |
| `voltage_limit` | 电压上限 (0=等于母线电压) | 0 |
| `speed_mode` | LEDC 速度模式 | `LEDC_LOW_SPEED_MODE` |
| `timer_num` | LEDC 定时器编号 | `LEDC_TIMER_0` |
| `channel_u/v/w` | LEDC 通道 | 0/1/2 |
| `pwm_freq_hz` | PWM 频率 | 20000 Hz |
| `duty_resolution` | 占空比分辨率 | `LEDC_TIMER_8_BIT` (0~255) |

**核心函数**:

| 函数 | 说明 |
|------|------|
| `bldc_motor_init()` | 初始化 GPIO + LEDC PWM |
| `bldc_motor_deinit()` | 停止 PWM + 复位 GPIO |
| `bldc_motor_enable()` | 拉高 EN 引脚 → PWM=0 |
| `bldc_motor_disable()` | PWM=0 → 拉低 EN 引脚 |
| `bldc_motor_set_duty()` | 直接设置三路 PWM 占空比 |
| `bldc_motor_set_phase_voltage()` | 电压→占空比转换后写入 |
| `bldc_motor_set_phase_state()` | 独立控制各相 EN 状态 |

**电压→占空比转换**: `duty = constrain(U / Vsupply, 0, 1) * max_duty`

#### 文件: `moter_driver/inc/bldc_foc_modulation.h` & `moter_driver/src/bldc_foc_modulation.c`

**角色**: FOC 电压输出到 PWM 信号的调制策略。

**支持的调制方式**:

| 调制类型 | 说明 | 特点 |
|----------|------|------|
| `SVPWM` (默认) | 空间矢量 PWM + Midpoint Clamp | 母线电压利用率 100% |
| `SinePWM` | 标准正弦 PWM | 母线电压利用率 86.6% |
| `Trapezoid_120` | 梯形波 120° (6 步换相) | 简单，转矩脉动大 |
| `Trapezoid_150` | 梯形波 150° (12 步换相) | 较平滑 |

**SVPWM / SinePWM 核心流程** (`s_foc_modulate`):

```
1. Park 逆变换: dq → αβ
   Uα = Ud*cos(θ) - Uq*sin(θ)
   Uβ = Ud*sin(θ) + Uq*cos(θ)

2. Clarke 逆变换: αβ → abc
   Ua = Uα
   Ub = -0.5*Uα + √3/2*Uβ
   Uc = -0.5*Uα - √3/2*Uβ

3. SVPWM 零序注入 (仅 SVPWM 模式):
   center = Vbus/2 - (Umax + Umin)/2
   Ua += center, Ub += center, Uc += center

4. 写入硬件: bldc_motor_set_phase_voltage(Ua, Ub, Uc)
```

**梯形波模式**: 根据电角度查找换相表，直接输出固定电平 + 使能控制。

**居中 vs 非居中调制**:
- 居中: 三相电压偏置到 Vbus/2，适合全桥驱动器
- 非居中: 最低相拉到 0V，适合低侧电流采样

---

### 3.5 传感器驱动层 (`sensors/`)

#### 文件: `sensors/inc/AS5600.h` & `sensors/src/AS5600.c`

**AS5600 磁编码器驱动**，通过 I2C 接口读取 12 位绝对角度值。

**默认配置**:

| 参数 | 值 |
|------|-----|
| I2C 端口 | `I2C_NUM_0` |
| SDA/SCL 引脚 | GPIO 1 / GPIO 2 |
| 设备地址 | 0x36 |
| I2C 速率 | 100 kHz |
| 速度滤波系数 α | 0.2 |
| 加速度滤波系数 α | 0.2 |

**采样更新 (`as5600_update`)**:

```
1. I2C 读取原始 12 位角度 → 转换为角度 (度)
2. 计算时间差 dt
3. 处理 0°↔360° 回绕
4. 瞬时速度 = Δ角度 / dt
5. 瞬时加速度 = Δ速度 / dt
6. 一阶低通滤波:
   speed = α * inst_speed + (1-α) * speed_prev
   accel = α * inst_accel + (1-α) * accel_prev
```

**内部状态** (静态单例 `s_ctx`):
- I2C 总线句柄、设备句柄
- 当前角度/速度/加速度缓存
- 上一帧数据 (用于差分计算)

**关键接口**:
- `as5600_update()` — 必须在控制循环中定时调用
- `as5600_get_angle_rad()` — 获取缓存的角度 (弧度)，无需 I2C 通信
- `as5600_get_speed_rad()` — 获取缓存的速度 (弧度/秒)
- `as5600_magnet_detected()` — 磁铁检测 (STATUS 寄存器 MD 位)

---

## 4. 数据流与控制流

### 4.1 运行时控制流

```
FreeRTOS Task (1kHz)
    │
    ▼
motor_control_step(mc)
    │
    ├──► foc_loop_foc(&mc->foc)           ← 电流环 (每次执行)
    │       │
    │       ├──► sensor_update()           ← 通过回调调用 as5600_update()
    │       ├──► foc_electrical_angle()    ← 计算电角度
    │       ├──► [力矩计算: 电压/电流PI]   ← 计算 Vd, Vq
    │       └──► set_phase_voltage()       ← 通过回调调用 bldc_foc_set_voltage()
    │               │
    │               ├──► Park⁻¹ (dq → αβ)
    │               ├──► Clarke⁻¹ (αβ → abc)
    │               ├──► SVPWM 零序注入
    │               └──► bldc_motor_set_phase_voltage()
    │                       │
    │                       └──► LEDC PWM 输出到三相引脚
    │
    └──► foc_move(&mc->foc, NAN)          ← 运动外环 (每10次执行一次)
            │
            ├──► foc_shaft_angle()         ← 读取传感器角度
            ├──► foc_shaft_velocity()      ← 读取传感器速度 + LPF
            │
            └──► [级联 PID]
                    torque:    target → current_sp
                    velocity:  target → PID_vel → current_sp
                    angle:     target → PID_angle → vel_sp → PID_vel → current_sp
```

### 4.2 初始化流程

```
app_main()
    │
    └──► motor_control_init(&mc)
            │
            ├──► sensor_init(SENSOR_TYPE_AS5600)
            │       ├──► sensor_ops_init_as5600()  ← 注入 AS5600 函数指针
            │       └──► as5600_init(NULL)         ← 初始化 I2C 总线
            │
            ├──► bldc_motor_init(&cfg)
            │       ├──► enable_gpios_init()       ← 配置使能 GPIO
            │       └──► ledc_pwm_init()           ← 配置 LEDC PWM
            │
            ├──► foc_init(&foc, &params)
            │       ├──► pid_init() x4             ← 初始化 4 个 PID
            │       └──► lpf_init() x4             ← 初始化 4 个 LPF
            │
            └──► foc_inject_callbacks()            ← 注入平台回调

motor_control_calibrate(mc)
    └──► foc_init_foc(&mc->foc)
            ├──► foc_align_sensor()                ← 方向检测 + 电零点
            └──► foc_align_current_sense()         ← 电流传感器对齐
```

---

## 5. 文件清单与作用说明

| 文件 | 层 | 作用 |
|------|-----|------|
| `main/main.c` | 应用入口 | 程序入口，组装并启动控制系统 |
| `main/CMakeLists.txt` | 构建 | 注册 main 组件，依赖 motor_control |
| `CMakeLists.txt` | 构建 | 根项目定义 |
| **集成控制模块 (icm/)** | | |
| `icm/inc/motor_control.h` | 接口 | 对外暴露的统一 API (类型、函数声明) |
| `icm/src/motor_control.c` | 实现 | 组装各模块，注入回调，实现控制接口 |
| `icm/inc/sensor.h` | 接口 | 传感器 HAL 接口 (函数指针表) |
| `icm/src/sensor.c` | 实现 | 根据类型注入驱动指针，转发调用 |
| **FOC 算法层 (algorithm/)** | | |
| `algorithm/inc/foc.h` | 接口 | FOC 核心数据结构 + API 声明 |
| `algorithm/src/foc.c` | 实现 | 电流环、运动外环、校准、参数辨识 |
| `algorithm/inc/foc_utils.h` | 接口 | 数学工具声明 (三角函数、限幅、归一化) |
| `algorithm/src/foc_utils.c` | 实现 | 查表 sin/cos、多项式 atan2、快速 sqrt |
| `algorithm/inc/pid.h` | 接口 | PID 控制器声明 |
| `algorithm/src/pid.c` | 实现 | Tustin 积分 PID + 抗饱和 + ramp 限幅 |
| `algorithm/inc/lowpass_filter.h` | 接口 | 一阶低通滤波器声明 |
| `algorithm/src/lowpass_filter.c` | 实现 | 一阶 IIR 低通滤波 |
| `algorithm/inc/foc_defaults.h` | 配置 | 全部默认参数宏定义 |
| **电机驱动层 (moter_driver/)** | | |
| `moter_driver/inc/bldc_motor.h` | 接口 | BLDC 电机驱动声明 (LEDC PWM + GPIO) |
| `moter_driver/src/bldc_motor.c` | 实现 | LEDC PWM 配置、电压→占空比、使能控制 |
| `moter_driver/inc/bldc_foc_modulation.h` | 接口 | FOC 调制策略声明 (SVPWM/正弦/梯形) |
| `moter_driver/src/bldc_foc_modulation.c` | 实现 | Park/Clarke 逆变换 + SVPWM 零序注入 |
| **传感器驱动层 (sensors/)** | | |
| `sensors/inc/AS5600.h` | 接口 | AS5600 磁编码器驱动声明 |
| `sensors/src/AS5600.c` | 实现 | I2C 读取、角度/速度/加速度计算 |
| **其他** | | |
| `components/motor_control/CMakeLists.txt` | 构建 | 组件注册，声明所有源文件与依赖 |
| `FOC_基本原理.md` | 文档 | FOC 理论基础 (Clarke/Park 变换、控制框图) |

---

## 6. 核心代码解析

### 6.1 控制模式切换 (`foc_set_control_type`)

切换控制模式时的**无扰切换**策略：
- 切换到角度模式时，将当前实际角度设为 `target`，避免跳变
- 切换到速度模式时，`target = 0`
- 切换到力矩模式时，`target = 0`
- 切换后自动同步各 PID 的限幅参数

### 6.2 力矩控制类型详解

| 类型 | 需要的硬件 | 工作原理 |
|------|-----------|---------|
| `VOLTAGE` | 无 | 直接设置 Vq = target, Vd = 0 (Id=0 策略) |
| `ESTIMATED_CURRENT` | 电机参数 | Vq = I*R + BEMF, Vd = -I*ω*Lq |
| `DC_CURRENT` | 电流传感器 | 单 PI 控制 q 轴电流 |
| `FOC_CURRENT` | 电流传感器 | 双 PI 控制 dq 电流 + 交叉耦合补偿 |

本项目当前仅使用 `VOLTAGE` 模式，无电流传感器。

### 6.3 传感器对齐算法 (`foc_align_sensor`)

```
步骤 1: 索引搜索 (如编码器支持)
    → 开环旋转直到找到索引信号

步骤 2: 方向检测
    → 电角度从 3π/2 到 3π/2+2π 旋转 (正转)
    → 记录中间角度 mid_angle
    → 电角度从 3π/2+2π 到 3π/2 旋转 (反转)
    → 记录结束角度 end_angle
    → mid_angle < end_angle → CCW, 否则 → CW

步骤 3: 极对数校验
    → 实际转动角度 × pole_pairs ≈ 2π → 校验通过
    → 否则输出估算极对数

步骤 4: 电零点校准
    → 在 3π/2 电角度通电 700ms 锁定转子
    → 读取当前传感器值作为 zero_electric_angle
```

### 6.4 角度回绕处理 (AS5600)

AS5600 输出 0~360° 的绝对角度，在跨 0° 时需要处理回绕：

```c
float d = angle - last_angle;
if (d > 180.0f)       d -= 360.0f;  // 正方向过零
else if (d < -180.0f) d += 360.0f;  // 反方向过零
```

### 6.5 快速三角函数 (`foc_sin`)

采用 **65 元素查表 + 8bit 线性插值** 实现，利用正弦波的对称性将表压缩为 1/4 周期：

```
输入角度 a → 归一化到 [0, 2π) → 量化为 16bit 索引
  ├── [0, π/2):   查表 + 插值
  ├── [π/2, π):   对称映射 sin(x) = sin(π-x)
  ├── [π, 3π/2):  sin(x) = -sin(x-π)
  └── [3π/2, 2π): sin(x) = -sin(2π-x)
```

---

## 7. FOC 基本原理概述

> 详见项目根目录 `FOC_基本原理.md`，此处做简要总结。

### 7.1 核心思想

**用坐标变换把三相交流电机等效为直流电机来控制。**

```
三相交流 (a,b,c)  ──Clarke──►  两相静止 (α,β)  ──Park──►  两相旋转 (d,q)
  正弦波                        正弦波+余弦波                  直流恒定值
```

### 7.2 d-q 轴物理含义

| 轴 | 方向 | 作用 |
|----|------|------|
| d 轴 (direct) | 与永磁体磁场同轴 | 控制磁场强弱 |
| q 轴 (quadrature) | 与永磁体磁场正交 | 产生转矩 |

**Id=0 控制策略**: d 轴电流为零，所有电流用于产生转矩，适用于表贴式永磁电机。

### 7.3 控制环路

```
位置环 (最外层, 最慢)
    ↓
速度环 (中间层)
    ↓
电流环 (最内层, 最快, = FOC 环)
    ↓
SVPWM → 三相逆变器 → 电机
```

- 电流环: 10-50kHz (PWM 频率)
- 速度环: 1-5kHz
- 位置环: 100-500Hz

本项目中电流环 = 控制任务频率 1kHz，运动外环 = 100Hz。

### 7.4 SVPWM

空间矢量 PWM 通过零序分量注入，将母线电压利用率从 SPWM 的 86.6% 提升到 100%：

```
Ua_centered = Ua + (Vbus/2 - (Umax + Umin)/2)
Ub_centered = Ub + (Vbus/2 - (Umax + Umin)/2)
Uc_centered = Uc + (Vbus/2 - (Umax + Umin)/2)
```

---

## 8. 构建系统说明

### 8.1 组件依赖关系

```
main
  └── REQUIRES: motor_control

motor_control (组件)
  └── REQUIRES: esp_driver_ledc    (LEDC PWM 驱动)
                esp_driver_gpio    (GPIO 驱动)
                esp_driver_i2c     (I2C 驱动, AS5600)
                esp_timer          (高精度定时器)
                driver             (传统驱动 API)
```

### 8.2 源文件编译列表

`components/motor_control/CMakeLists.txt` 中注册的源文件：

```
moter_driver/src/bldc_motor.c          — 电机驱动
moter_driver/src/bldc_foc_modulation.c — FOC 调制
sensors/src/AS5600.c                   — 磁编码器
algorithm/src/foc.c                    — FOC 核心
algorithm/src/foc_utils.c              — 数学工具
algorithm/src/pid.c                    — PID 控制器
algorithm/src/lowpass_filter.c         — 低通滤波
icm/src/motor_control.c                — 集成控制
icm/src/sensor.c                       — 传感器抽象
```

### 8.3 编译配置

- PWM 频率: 20kHz (`bldc_motor_config_t.pwm_freq_hz`)
- 控制频率: 1kHz (`foc_control_task` 中的 `pdMS_TO_TICKS(1)`)
- 运动环频率: 100Hz (每 10 次电流环执行一次)

---

> **文档版本**: v1.0  
> **生成日期**: 2026-08-02  
> **适用项目**: motor_control_1.0 (ESP32 BLDC FOC 控制)
