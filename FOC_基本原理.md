# 电机 FOC（磁场定向控制）基本原理

FOC（Field-Oriented Control），又称矢量控制，是目前无刷电机最主流的控制算法。下面从核心思想逐步展开。

---

## 1. 为什么要 FOC？

### 传统方波驱动的痛点

普通六步换向（方波驱动）只在 6 个固定角度切换电流，电磁力方向不连续，导致：
- **转矩脉动大**：每 60° 电角度转矩波动一次
- **低速抖动**：步进感明显
- **效率低**：电流方向与磁场方向未对齐

### FOC 的目标

**让定子磁场始终与转子磁场保持 90° 正交**，像骑自行车一样——脚踩踏板的力量始终垂直于曲柄，每一分力都用来产生旋转。

---

## 2. 核心数学变换（Clarke → Park）

FOC 的精髓是**把三相交流电机等效为直流电机来控制**。

```
三相交流 (a,b,c)  ──Clarke──►  两相静止 (α,β)  ──Park──►  两相旋转 (d,q)
  正弦波                      正弦波+余弦波                直流恒定值！
```

### 2.1 Clarke 变换（三相 → 两相静止）

把 120° 分布的三个线圈电流，投影到直角坐标系：

$$
\begin{bmatrix} I_\alpha \\ I_\beta \end{bmatrix} = \frac{2}{3} \begin{bmatrix} 1 & -\frac{1}{2} & -\frac{1}{2} \\ 0 & \frac{\sqrt{3}}{2} & -\frac{\sqrt{3}}{2} \end{bmatrix} \begin{bmatrix} I_a \\ I_b \\ I_c \end{bmatrix}
$$

结果：`Iα` 和 `Iβ` 是两路**正弦波**，相位差 90°。

### 2.2 Park 变换（两相静止 → 两相旋转）

关键一步：让坐标系**随转子磁场一起旋转**！

$$
\begin{bmatrix} I_d \\ I_q \end{bmatrix} = \begin{bmatrix} \cos\theta_e & \sin\theta_e \\ -\sin\theta_e & \cos\theta_e \end{bmatrix} \begin{bmatrix} I_\alpha \\ I_\beta \end{bmatrix}
$$

- `θe` = 转子电角度（需要编码器测量）
- 结果：**旋转的交流量变成静止的直流量！**

---

## 3. d-q 轴的物理含义

| 轴 | 方向 | 作用 | 类比 |
|-----|------|------|--------|
| **d 轴** (direct) | 与永磁体磁场同轴 | 控制磁场强弱（励磁） | 直流电机的励磁电流 |
| **q 轴** (quadrature) | 与永磁体磁场正交 | 产生转矩 | 直流电机的电枢电流 |

类比直流电机：

```
FOC 控制 PMSM/BLDC   ≈   控制一台他励直流电机
    Iq  ←→  电枢电流（产生转矩）
    Id  ←→  励磁电流（控制磁场）
```

**转矩公式**（SPMSM，表贴式永磁电机）：

$$T = \frac{3}{2} \cdot P_p \cdot \psi_f \cdot I_q$$

- `Pp` = 极对数，`ψf` = 永磁体磁链（常数）
- **转矩正比于 Iq！**——这就是为什么控制 Iq 就能控制转矩。

---

## 4. FOC 的完整控制环路

```
                ┌─────────────────────────────────────────────────────────┐
                │                     FOC 控制框图                         │
                │                                                         │
  ω_ref ──►[PI]──► Iq_ref ──►[PI]──► Vq ──┐                              │
                │                          │                              │
   Id_ref=0 ────►[PI]──► Vd ──────────────┤                              │
                │                          ▼                              │
                │              ┌─ Park⁻¹ ──► SVPWM ──► 三相逆变器 ──► 电机 │
                │              │                                          │
                │  编码器 ──► θe ──► Park ──► Ia,Ib,Ic                    │
                │                    (电流采样)                             │
                └─────────────────────────────────────────────────────────┘
```

### 电流环（内环，最快）

```
目标 Iq/Id ──►[PI 控制器]──► Vq/Vd ──► Park⁻¹ ──► SVPWM ──► 电机
                 ↑
实际 Iq/Id ←── Park ←── Clarke ←── 三相电流采样
```

- 频率：通常 10-50 kHz（PWM 频率）
- 作用：让实际电流跟踪目标电流

### 速度环（外环，较慢）

```
目标速度 ──►[PI 控制器]──► Iq_ref ──► 电流环 ──► 电机
              ↑
实际速度 ←── 编码器/霍尔
```

- 频率：通常 1-5 kHz
- 作用：让实际转速跟踪目标转速

### 位置环（最外环，最慢）

```
目标角度 ──►[P 控制器]──► 速度指令 ──► 速度环 ──► ...
              ↑
实际角度 ←── 编码器
```

---

## 5. 结合代码理解

项目中，这些变换已经封装好：

```c
// Park 逆变换 + SVPWM（电压 → 三相 PWM 占空比）
setPhaseVoltage(Uq, 0, _electricalAngle(shaft_angle, pole_pairs));
//              ↑   ↑                ↑
//              |   Ud=0             电角度
//              q 轴电压（产生转矩）
```

`Ud = 0` 就是 **Id=0 控制策略**（Maximum Torque Per Ampere 的最简单形式），所有电压都用来产生转矩，适用于表贴式永磁电机。

---

## 6. SVPWM（空间矢量调制）

Park 逆变换得到 `Vα, Vβ` 后，不是直接做 PWM 比较，而是使用 SVPWM：

| 方法 | 直流电压利用率 | 特点 |
|------|:---:|------|
| SPWM | 86.6% | 简单正弦波比较 |
| **SVPWM** | **100%** | 构造旋转电压矢量，利用率提高 15% |

SVPWM 把 6 个开关管分为 8 种状态（6 个有效 + 2 个零矢量），通过相邻矢量的时间分配合成任意方向的电压矢量。

---

## 7. 关键总结

| 概念 | 一句话 |
|------|--------|
| **FOC 本质** | 用坐标变换把交流电机变成直流电机来控制 |
| **Clarke** | 3 相 → 2 相静止（去冗余） |
| **Park** | 2 相静止 → 2 相旋转（去交流） |
| **d 轴** | 磁场方向，控制励磁 |
| **q 轴** | 正交方向，控制转矩 |
| **Id=0** | 最简单的控制策略，力矩最大化 |
| **PI 控制器** | 闭环调节的核心，让实际值跟踪目标值 |

FOC 的优雅之处在于：**用一个旋转的坐标系消去了所有交流量，剩下的就是直流直流的控制问题**——把困难的非线性控制变成了线性 PI 调节。
